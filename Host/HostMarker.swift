import Foundation

/// Placeholder entry point for the Xcode host target; replaced by `scripts/xcode-copy-host-app.sh` after linking.
@main
enum HostMain {
    static func main() {
        fputs("Terminal Manager host stub\n", stderr)
    }
}
